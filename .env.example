# Keep this empty when the frontend and Laravel API use the same pbxpro.test origin.
VITE_API_URL=
# Use false for authenticated Super Admin, Tenant Admin, and Agent portals.
# Use true only when previewing the mock dashboards without backend authentication.
VITE_DEMO_MODE=false
